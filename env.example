# Alpha-Trade Infrastructure Configuration Template
# ===================================================
# 复制此文件为 env.example 并填写实际值。

# --- 系统基础 (System Base) ---
APP_ENV=development # production, testing, development
SERVICE_NAME=alpha-trade
LOG_LEVEL=info

# --- 安全与加密 (Security & Encryption) ---
# [重要] 用于加密数据库中 exchange_accounts 表的 api_secret。
# 生产环境建议使用 32 位随机字符串 (AES-256)。
APP_ENCRYPTION_KEY=your-super-secret-32-char-key-here

# --- 数据库 (Database) ---
DB_URL=postgres://postgres:123456@localhost:5432/alpha_trade?sslmode=disable

# --- 消息队列 (NATS) ---
NATS_URL=nats://localhost:4222

# --- 邮件服务 (AWS SES) ---
AWS_REGION=us-east-1
AWS_SES_ACCESS_KEY=AKIA...
AWS_SES_SECRET_KEY=wJalrXUtnFEMI...

# --- 身份认证 (Authentication) ---
AUTH_SECRET=your-base-access-secret-here
AUTH_MFA_SECRET=your-mfa-step-up-secret-here
AUTH_EXPIRE=86400
AUTH_MFA_EXPIRE=600

# --- 管理员初始化 (Admin Bootstrap) ---
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=change-me-immediately-on-first-login

# --- 交易配置 (Trading) ---
# 是否启用交易功能（默认 false，避免误启动）
TRADING_ENABLED=false
# 交易模式: auto(自动启动) / manual(手动触发) / hybrid(混合模式)
TRADING_MODE=hybrid
# 交易对列表（逗号分隔，如: BTCUSDT,ETHUSDT）
TRADING_SYMBOLS=BTCUSDT
# K线周期（如: 1m, 5m, 15m, 1h, 1d）
TRADING_KLINE_INTERVAL=1m
# 策略类型（当前支持: simple_volatility）
TRADING_STRATEGY_TYPE=simple_volatility

# --- Binance API 配置 ---
# Binance API Key（启用交易时必须配置）
BINANCE_API_KEY=your-binance-api-key
# Binance API Secret（启用交易时必须配置）
BINANCE_API_SECRET=your-binance-api-secret
# 是否使用测试网（默认 true，生产环境需显式设置为 false）
BINANCE_TESTNET=true

# --- 风控配置 (Risk Management) ---
# 风控状态存储类型: redis 或 postgres（默认 redis）
RISK_REPO_TYPE=redis

# --- Redis 配置（如果使用 Redis RiskRepo） ---
REDIS_URL=redis://localhost:6379/0
