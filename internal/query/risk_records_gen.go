// Code generated by gpmg. DO NOT EDIT.
// generated_at_utc: 2026-01-18T14:25:30Z
// version: 1.0.0

package query

import (
	"context"
	"database/sql"
	"fmt"
	"github.com/Masterminds/squirrel"
	"strings"
	"time"
)

// RiskRecords represents a row in table "public"."risk_records".
//
// Indexes:
//   - risk_records_pkey (PRIMARY KEY) (btree): id
type RiskRecords struct {
	ID          int64     `db:"id" json:"id"`                     // 流水 ID
	EventType   string    `db:"event_type" json:"event_type"`     // 事件类型 [ENUM: POS_LIMIT, LOSS_CIRCUIT, SLIPPAGE, PRICE_PROTECT]
	Level       string    `db:"level" json:"level"`               // 风险级别 [ENUM: INFO, WARNING, BLOCK, CRITICAL]
	Symbol      string    `db:"symbol" json:"symbol"`             // 涉及标的
	StrategyID  string    `db:"strategy_id" json:"strategy_id"`   // 涉及策略
	Details     string    `db:"details" json:"details"`           // 触发时的上下文快照 [JSON]
	TriggeredAt time.Time `db:"triggered_at" json:"triggered_at"` // 触发时间
}

// TableName returns the table name for RiskRecords
func (m *RiskRecords) TableName() string {
	return "public.risk_records"
}

// RiskRecordsIndex represents indexed columns for covering index queries.
// Only includes columns that appear in at least one index, enabling index-only scans.
type RiskRecordsIndex struct {
	ID int64 `db:"id" json:"id"` // 流水 ID
}

// Create inserts a new record and returns the inserted record with auto-generated fields
func (d *riskRecordsDo) Create(ctx context.Context, model *RiskRecords) (*RiskRecords, error) {
	builder := squirrel.Insert(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Columns("event_type", "level", "symbol", "strategy_id", "details", "triggered_at").
		Values(model.EventType, model.Level, model.Symbol, model.StrategyID, model.Details, model.TriggeredAt).
		Suffix("RETURNING " + strings.Join(d.columns, ", "))

	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build insert query: %w", err)
	}

	var result RiskRecords
	err = d.db.QueryRowContext(ctx, query, args...).Scan(
		&result.ID,
		&result.EventType,
		&result.Level,
		&result.Symbol,
		&result.StrategyID,
		&result.Details,
		&result.TriggeredAt,
	)
	if err != nil {
		return nil, fmt.Errorf("insert: %w", err)
	}
	return &result, nil
}

// BatchCreate inserts multiple records and returns all inserted records
func (d *riskRecordsDo) BatchCreate(ctx context.Context, models []*RiskRecords) ([]*RiskRecords, error) {
	if len(models) == 0 {
		return nil, nil
	}

	builder := squirrel.Insert(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Columns("event_type", "level", "symbol", "strategy_id", "details", "triggered_at")

	for _, model := range models {
		builder = builder.Values(model.EventType, model.Level, model.Symbol, model.StrategyID, model.Details, model.TriggeredAt)
	}

	builder = builder.Suffix("RETURNING " + strings.Join(d.columns, ", "))
	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build batch insert query: %w", err)
	}

	rows, err := d.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, fmt.Errorf("batch insert: %w", err)
	}
	defer rows.Close()

	var results []*RiskRecords
	for rows.Next() {
		var result RiskRecords
		err := rows.Scan(
			&result.ID,
			&result.EventType,
			&result.Level,
			&result.Symbol,
			&result.StrategyID,
			&result.Details,
			&result.TriggeredAt,
		)
		if err != nil {
			return nil, fmt.Errorf("scan batch insert result: %w", err)
		}
		results = append(results, &result)
	}
	return results, rows.Err()
}

// FindByPK finds a record by primary key
func (d *riskRecordsDo) FindByPK(ctx context.Context, iD int64) (*RiskRecords, error) {
	builder := squirrel.Select(d.columns...).
		From(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Where(squirrel.Eq{
			"id": iD,
		}).
		Limit(1)

	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build select query: %w", err)
	}

	var result RiskRecords
	err = d.db.QueryRowContext(ctx, query, args...).Scan(
		&result.ID,
		&result.EventType,
		&result.Level,
		&result.Symbol,
		&result.StrategyID,
		&result.Details,
		&result.TriggeredAt,
	)
	if err == sql.ErrNoRows {
		return nil, ErrRecordNotFound
	}
	if err != nil {
		return nil, fmt.Errorf("find by pk: %w", err)
	}
	return &result, nil
}

// UpdateByPK updates a record by primary key
func (d *riskRecordsDo) UpdateByPK(ctx context.Context, model *RiskRecords) error {
	builder := squirrel.Update(d.table).
		PlaceholderFormat(squirrel.Dollar)
	builder = builder.Set("event_type", model.EventType)
	builder = builder.Set("level", model.Level)
	builder = builder.Set("symbol", model.Symbol)
	builder = builder.Set("strategy_id", model.StrategyID)
	builder = builder.Set("details", model.Details)
	builder = builder.Set("triggered_at", model.TriggeredAt)
	builder = builder.Where(squirrel.Eq{
		"id": model.ID,
	})

	query, args, err := builder.ToSql()
	if err != nil {
		return fmt.Errorf("build update query: %w", err)
	}

	result, err := d.db.ExecContext(ctx, query, args...)
	if err != nil {
		return fmt.Errorf("update: %w", err)
	}

	affected, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("get rows affected: %w", err)
	}
	if affected == 0 {
		return ErrRecordNotFound
	}
	return nil
}

// DeleteByPK deletes a record by primary key
func (d *riskRecordsDo) DeleteByPK(ctx context.Context, iD int64) error {
	builder := squirrel.Delete(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Where(squirrel.Eq{
			"id": iD,
		})

	query, args, err := builder.ToSql()
	if err != nil {
		return fmt.Errorf("build delete query: %w", err)
	}

	result, err := d.db.ExecContext(ctx, query, args...)
	if err != nil {
		return fmt.Errorf("delete: %w", err)
	}

	affected, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("get rows affected: %w", err)
	}
	if affected == 0 {
		return ErrRecordNotFound
	}
	return nil
}

// Upsert inserts or updates a record (uses ON CONFLICT for PostgreSQL)
// Only updates non-zero fields on conflict
func (d *riskRecordsDo) Upsert(ctx context.Context, model *RiskRecords) (*RiskRecords, error) {
	builder := squirrel.Insert(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Columns("event_type", "level", "symbol", "strategy_id", "details", "triggered_at").
		Values(model.EventType, model.Level, model.Symbol, model.StrategyID, model.Details, model.TriggeredAt)

	// Build update clause for conflict resolution
	updateClause := "ON CONFLICT (id) DO UPDATE SET "
	updateClause += "event_type = CASE WHEN EXCLUDED.event_type = '' THEN risk_records.event_type ELSE EXCLUDED.event_type END"
	updateClause += ", "
	updateClause += "level = CASE WHEN EXCLUDED.level = '' THEN risk_records.level ELSE EXCLUDED.level END"
	updateClause += ", "
	updateClause += "symbol = CASE WHEN EXCLUDED.symbol = '' THEN risk_records.symbol ELSE EXCLUDED.symbol END"
	updateClause += ", "
	updateClause += "strategy_id = CASE WHEN EXCLUDED.strategy_id = '' THEN risk_records.strategy_id ELSE EXCLUDED.strategy_id END"
	updateClause += ", "
	updateClause += "details = CASE WHEN EXCLUDED.details = '' THEN risk_records.details ELSE EXCLUDED.details END"
	updateClause += ", "
	updateClause += "triggered_at = EXCLUDED.triggered_at"

	builder = builder.Suffix(updateClause + " RETURNING " + strings.Join(d.columns, ", "))
	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build upsert query: %w", err)
	}

	var result RiskRecords
	err = d.db.QueryRowContext(ctx, query, args...).Scan(
		&result.ID,
		&result.EventType,
		&result.Level,
		&result.Symbol,
		&result.StrategyID,
		&result.Details,
		&result.TriggeredAt,
	)
	if err != nil {
		return nil, fmt.Errorf("upsert: %w", err)
	}
	return &result, nil
}

// UpsertAll inserts or updates a record, updating all fields on conflict
func (d *riskRecordsDo) UpsertAll(ctx context.Context, model *RiskRecords) (*RiskRecords, error) {
	builder := squirrel.Insert(d.table).
		PlaceholderFormat(squirrel.Dollar).
		Columns("event_type", "level", "symbol", "strategy_id", "details", "triggered_at").
		Values(model.EventType, model.Level, model.Symbol, model.StrategyID, model.Details, model.TriggeredAt)

	updateClause := "ON CONFLICT (id) DO UPDATE SET "
	updateClause += "event_type = EXCLUDED.event_type"
	updateClause += ", "
	updateClause += "level = EXCLUDED.level"
	updateClause += ", "
	updateClause += "symbol = EXCLUDED.symbol"
	updateClause += ", "
	updateClause += "strategy_id = EXCLUDED.strategy_id"
	updateClause += ", "
	updateClause += "details = EXCLUDED.details"
	updateClause += ", "
	updateClause += "triggered_at = EXCLUDED.triggered_at"

	builder = builder.Suffix(updateClause + " RETURNING " + strings.Join(d.columns, ", "))
	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build upsert all query: %w", err)
	}

	var result RiskRecords
	err = d.db.QueryRowContext(ctx, query, args...).Scan(
		&result.ID,
		&result.EventType,
		&result.Level,
		&result.Symbol,
		&result.StrategyID,
		&result.Details,
		&result.TriggeredAt,
	)
	if err != nil {
		return nil, fmt.Errorf("upsert all: %w", err)
	}
	return &result, nil
}

// FindByIndex performs a covering index query using only indexed columns.
// Automatically builds WHERE conditions for non-zero fields in the request.
// Returns only indexed columns to enable index-only scans (no table access).
func (d *riskRecordsDo) FindByIndex(ctx context.Context, req *RiskRecordsIndex) ([]*RiskRecordsIndex, error) {
	builder := squirrel.Select().
		From(d.table).
		PlaceholderFormat(squirrel.Dollar)

	// Auto-build WHERE conditions from non-zero fields
	if req.ID != 0 {
		builder = builder.Where(squirrel.Eq{"id": req.ID})
	}

	// Select only indexed columns (enables covering index scan)
	builder = builder.Columns("id")

	query, args, err := builder.ToSql()
	if err != nil {
		return nil, fmt.Errorf("build find by index query: %w", err)
	}

	rows, err := d.db.QueryContext(ctx, query, args...)
	if err != nil {
		return nil, fmt.Errorf("execute find by index: %w", err)
	}
	defer rows.Close()

	var results []*RiskRecordsIndex
	for rows.Next() {
		var result RiskRecordsIndex
		err := rows.Scan(
			&result.ID,
		)
		if err != nil {
			return nil, fmt.Errorf("scan index result: %w", err)
		}
		results = append(results, &result)
	}
	return results, rows.Err()
}
